{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "test": {
      "inputs": ["tests/**", "plugins/**/hooks/**", "vitest.config.*"],
      "outputs": []
    },
    "lint": {
      "inputs": ["tests/**"],
      "outputs": []
    },
    "fmt:check": {
      "outputs": []
    }
  }
}
